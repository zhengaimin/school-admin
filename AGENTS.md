# Aimin-skill

<!-- aimin-skill-version: 0.1.2 -->

本文件作为项目规则入口，只保留通用工作边界与规则路由；具体规范在命中场景后按需读取 `.agent/**` 文件。

## 工作方式

### 读取规则

- 先检查当前任务、目录结构和相关代码，再决定需要读取的规则文件。
- 默认不预读全部 `.agent/**`；命中具体场景后，再按路由表读取对应文件。
- 项目侧规则优先级高于通用模板；规则文件缺失时，优先遵守当前代码风格与本文件约束。

### 修改边界

- 只实现当前任务需要的最小改动，不添加未请求的功能、抽象、配置化或未来扩展点。
- 只修改与任务直接相关的文件；不顺手重构、格式化、改写注释、删除旧代码或清理无关问题。
- 只清理本次改动造成的未使用导入、变量和函数；发现无关问题时，在交付说明中单独指出。

### 代码注释

- 新增或修改代码时，同步检查注释是否需要补充、调整或删除；具体范围、格式和边界以 `.agent/comment.md` 为准。

### 文档与收尾

- 涉及第三方库、框架、SDK 或依赖 API，且存在版本差异或不确定时，先使用 Context7 MCP 查询最新文档。
- 只要本次会话修改了代码，收尾时必须按 `.agent/scripts/lint.md` 对本次修改文件执行 lint；优先按文件路径或最小范围执行，不默认全量执行。

## 路由表

| 场景                       | 读取文件                                                                                | 说明                             |
| -------------------------- | --------------------------------------------------------------------------------------- | -------------------------------- |
| 接口新增、接口更新         | 当前接口目录、`.agent/api.md`、`.agent/index/constants.json`、`.agent/index/utils.json` | 接口落点、类型命名、枚举协同     |
| 系统常量、枚举维护         | `.agent/index/constants.json`                                                           | 常量索引、文件位置、枚举值摘要   |
| 公共方法、工具函数         | `.agent/index/utils.json`                                                               | 公共方法索引、用途、文件位置     |
| 注释规范                   | 当前项目现有注释风格、`.agent/comment.md`                                               | 注释语言、格式与使用边界         |
| 命名、代码顺序             | 当前项目现有实现、`.agent/naming.md`                                                    | 通用命名与代码组织规范           |
| Admin 后台页面、表格、弹窗 | `.agent/admin/rules.md`、`.agent/admin/table.md`、`.agent/admin/modal.md`               | Vue 后台管理页面、表格和弹窗规则 |
| lint、交付收尾             | `.agent/scripts/lint.md`                                                                | 收尾检查与交付前清单             |

# school-admin

> Vue 3 + TypeScript + Vite 后台管理项目

## 项目约束（按需读）

| 文件                                  | 内容                                 |
| ------------------------------------- | ------------------------------------ |
| `.am-wf/context/base.md`              | 项目配置（技术栈、约束、设备信息等） |
| `.am-wf/context/native.md`            | 原生能力配置（可选）                 |
| `.am-wf/context/command-reference.md` | 本地命令快照与项目覆写               |

## 项目配置

> 详见 `.am-wf/context/base.md`

- 框架：`Vue 3`
- 语言：`TypeScript`
- 构建：`Vite`
- 主要业务域：权限、设备、公话日志、学生/家长、资金与套餐

## 项目约束

- 代码是真相，docs 只记录代码看不出来的东西
- 命中公共方法/常量变更，或怀疑当前代码已有可复用实现时，执行 `aimin --index` 检查 `.am-wf/index/*.json`
- 项目特有规范按需补充；与通用规则冲突时以本项目为准
- 提交前门禁仍以 `npm run aimin:gate` 为准，本次迁移只调整 AI 上下文入口结构
- 文档读取统一走 `.am-wf/context/base.md` + `.am-wf/index/*.json`，不再使用旧的复制式命令列表作为事实源
