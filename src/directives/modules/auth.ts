/**
 * v-auth
 * 按钮权限指令
 * @note 当前版本所有权限已放开，不会移除任何元素
 */
import type { Directive } from "vue";

const auth: Directive = {
  mounted() {
    // 当前版本所有权限已放开，不做任何处理
    return;
  }
};

export default auth;
