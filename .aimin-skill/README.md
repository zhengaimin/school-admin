# .aimin-skill

统一承载项目协作上下文与文档。

## 目录

- `context/`：原 `ai/` 内容
- `doc/`：原 `doc` 内容

## 维护规则

1. 新内容优先写入 `.aimin-skill/context` 与 `.aimin-skill/doc`。
2. 目录命名统一使用 `.aimin-skill` 与 `context`，不再使用 `.context` 与 `.ai`。
